# Sample Output

> 占位文件。把这一段引文删掉，换成 `sample-input.md` 对应的期望输出。
> 这是 Agent 模仿的"参考答案" —— 写得越像真实交付，模仿效果越好。

## 期望输出

```text
<在这里贴上你期望 Agent 产出的完整结果，包括格式、语气、长度>
```

## 为什么这样输出

简单解释这个输出体现了 `SKILL.md` 里的哪些约定，比如：

- 标题严格按"【会议总结】<会议名>·<日期>"
- 要点控制在 3-5 个，不过度展开
- 行动项必须含"责任人 + 事项 + 截止日期"三要素
